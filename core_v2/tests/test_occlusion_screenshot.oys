LEVEL res://core_v2/tests/test_occlusion_debug.tscn

# Wait for scene load
WAIT 1s

# Walk forward into the occlusion area and behind the wall
# Speed 2.0m/s. Distance to -6.0 is 6m. Duration 3s.
FW 3.0s

# Wait a bit for everything to settle
WAIT 1s

# Take screenshot
SCREENSHOT res://occlusion_test_result.png

END
