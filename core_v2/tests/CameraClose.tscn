[gd_scene load_steps=2 format=2]

[sub_resource type="GDScript" id=1]
script/source = "extends Spatial

func _ready():
	var cam = get_node_or_null(\"/root/PropStage/Camera\")
	if cam:
		cam.global_transform.origin = Vector3(0, 1.2, -1.8)
		cam.look_at(Vector3(0, 0.5, 0), Vector3.UP)
		print(\"[CameraClose] Moved camera to close-up position\")
	else:
		print(\"[CameraClose] Could not find PropStage/Camera\")
"

[node name="CameraClose" type="Spatial"]
script = SubResource( 1 )
