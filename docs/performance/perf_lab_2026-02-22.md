# Perf Lab Archive (2026-02-22)

Branch archived as failed experiment:

- `archive/perf-lab-failed-20260222`

## Scope tested

- Shader warmup A/B on `BaseTerrace`.
- Pushable box distance hibernation A/B.
- Windowed runs with GPU path and uncapped mode (`vblank_mode=0`).

## Representative results

Main vs perf-lab (same adjusted OYS path, windowed uncapped, `RUNS=1`):

- `main` best sample: `avg_fps=225.23` (`warmup_on`)
- `perf-lab` best sample: `avg_fps=224.69` (`warmup_on`)
- Delta: perf-lab ~`-0.24%` vs main (practical tie, no win)

Pushable box hibernation toggle (same route, uncapped):

- `hibernation_off`: `avg_fps=225.50`
- `hibernation_on`: `avg_fps=201.70`
- Delta: `-10.55%` with hibernation enabled in that scenario

## Verdict

- Keep A/B infra and docs.
- Do not ship perf-lab runtime tuning as a default optimization.
- Treat pushable box hibernation as rejected for current route until redesigned.
