experimental/
ports/
maps/autosave/

# Godot-specific ignores
export.cfg

# NOTE: .import files and test .json snapshots are in .gitignore but
# a post-commit hook auto-commits them separately for CI reproducibility.
# Run `git add -f <file>` to manually add them if needed.
.import/*
**/.import/
*.import
*.stex

# Test snapshots (auto-committed by post-commit hook)
core_v2/tests/*.json

.vscode/

# Imported translations (automatically generated from CSV files)
*.translation

# Mono-specific ignores
.mono/
data_*/
mono_crash.*.json

# Game specific
replays/
reports/

pupeteer/.venv/
*.pyc
*/__pycache__/
digest.txt
